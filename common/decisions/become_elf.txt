find_realm_lord_sigil_decision = {
    picture = {
        reference = "gfx/interface/illustrations/decisions/decision_find_realm_lord_sigil.dds"
    }
    decision_group_type = elf

    desc = find_realm_lord_sigil_decision_desc
    selection_tooltip = find_realm_lord_sigil_decision_tooltip

    ai_goal = no
    ai_will_do = {
        value = 0
    }

    ai_check_interval = 0

    is_shown = {
        is_ruler = yes
        # is_landed = yes

        NOT = {  #Can only do it once.
            is_target_in_global_variable_list = {
                name = unavailable_unique_decisions
                target = flag:find_realm_lord_sigil_decision
            }
        }
    }

    # Following conditions must be true to take decision, shown in requirements section
    is_valid = {

    }

    # Following conditions Will be shown as a validation error tooltip when False
    is_valid_showing_failures_only = {
        is_capable_adult = yes
        is_imprisoned = no
        is_at_war = no
    }

    cost = {
        # TODO: implement side quests to reduce the price
        # gold = 3000
        # gold = 3
    }

    effect = {
        show_as_tooltip = {
            # TODO: see if this works
            # restore_the_grand_portal_scripted_effect = yes
        }
        trigger_event = {
            id = elf_destiny_main_story_events.001
        }
        hidden_effect = {

        }
    }

    ai_potential = {
        always = no
    }

    ai_will_do = {
        base = 0
    }
}