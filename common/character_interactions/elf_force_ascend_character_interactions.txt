## Can be used to reboot matchmaking after pausing it
elf_force_ascend_interaction = {
    category = interaction_category_religion
    interface_priority = 60
    icon = icon_aeluran_sister

    desc = elf_force_ascend_interaction_desc
    use_diplomatic_range = no
    common_interaction = yes

    is_shown = {
        scope:actor = {
            is_ai = no
            is_any_elf_type = yes
        }
        scope:recipient = {
            is_high_elf = no
        }
    }

    cooldown = { months = 1 }

    on_accept = {
        scope:actor = {
            trigger_event = elf_force_ascend.001
        }
    }

    cost = {
        piety = {
            scope:recipient = {
                add = ritual_solo_cost
            }
        }
    }

    auto_accept = yes

    # ai_potential = {
    # 	is_ai = no
    # }
    ai_will_do = {
        base = 0
    }
}